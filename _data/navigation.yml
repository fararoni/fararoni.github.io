header:
  - titles:
      # @start locale config
      es      : &ES       Archivo
      en      : &EN       Archive
      # @end locale config
    url: /archive.html

  - titles:
      # @start locale config
      es      : &ES       Acerca de 
      en      : &EN       About
      # @end locale config
    url: /about.html
